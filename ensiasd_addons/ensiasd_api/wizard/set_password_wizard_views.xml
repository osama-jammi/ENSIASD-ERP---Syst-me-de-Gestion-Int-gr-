<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
    Fichier: ensiasd_addons/ensiasd_api/wizard/set_password_wizard_views.xml
    Vue du wizard pour définir manuellement le mot de passe API
    -->

    <!-- Vue formulaire du wizard -->
    <record id="view_set_api_password_wizard" model="ir.ui.view">
        <field name="name">set.api.password.wizard.form</field>
        <field name="model">set.api.password.wizard</field>
        <field name="arch" type="xml">
            <form string="Définir le mot de passe API">
                <group>
                    <field name="student_id" readonly="1"/>
                    <field name="password" password="True" placeholder="Saisir le mot de passe"/>
                    <field name="confirm_password" password="True" placeholder="Confirmer le mot de passe"/>
                </group>

                <div class="alert alert-info" role="alert">
                    <strong>💡 Astuce:</strong> Par défaut, le mot de passe est généré automatiquement au format <code>CNE+CIN</code>.<br/>
                    Utilisez ce formulaire uniquement si vous souhaitez définir un mot de passe personnalisé.
                </div>

                <footer>
                    <button name="action_set_password" string="Définir" type="object" class="btn-primary"/>
                    <button string="Annuler" class="btn-secondary" special="cancel"/>
                </footer>
            </form>
        </field>
    </record>

    <!-- Action -->
    <record id="action_set_api_password_wizard" model="ir.actions.act_window">
        <field name="name">Définir mot de passe API</field>
        <field name="res_model">set.api.password.wizard</field>
        <field name="view_mode">form</field>
        <field name="target">new</field>
    </record>
</odoo>